version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/Server/main07
permissions:
  - object: /home/ubuntu/Server/main07/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu